# Tuples is a built-in data type that lets us create immutable sequences of values.

tup = (1, 2, 3, 4, 2, 1, 2)
# print(type(tup))
# print(tup[3])

# print(tup.index(3))
# print(tup.count(2))

# Practice question
grade = ("A", "B", "A", "A", "C", "B", "D")

# print(grade.count('A'))

gradeList = ["A", "B", "A", "A", "C", "B", "D"]
gradeList.sort()
print(gradeList)
